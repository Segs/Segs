SET(target_CPP
${CMAKE_CURRENT_SOURCE_DIR}/BitStream.cpp
)
SET(target_INCLUDE
${CMAKE_CURRENT_SOURCE_DIR}/BitStream.h
)


SET(target_INCLUDE_DIR ${CMAKE_CURRENT_SOURCE_DIR})

SEGS_ADD_COMPONENT(GrowingBuffer SOURCE)

SET (target_SOURCES
${target_CPP}
${target_INCLUDE}
)

INCLUDE_DIRECTORIES(${target_INCLUDE_DIR})
INCLUDE_DIRECTORIES(${ACE_path})
INCLUDE_DIRECTORIES(${ZLIB_INCLUDE_DIR})

SEGS_REGISTER_COMPONENT(Bitstream "${target_INCLUDE_DIR}" "${target_CPP}" "${target_INCLUDE}" )
