# $Id: CMakeLists.txt 270 2006-10-01 18:33:49Z nemerle $

SET(UNICODE TRUE)
IF(UNICODE)
  ADD_DEFINITIONS(-D_UNICODE -UNICODE)
ENDIF(UNICODE)


FIND_PACKAGE(Threads REQUIRED)
FIND_PACKAGE(wxWidgets REQUIRED)
FIND_PACKAGE(OGRE REQUIRED)
FIND_PACKAGE(Boost)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR}/include)
INCLUDE_DIRECTORIES(${OGRE_path})
INCLUDE_DIRECTORIES(${ACE_path})
INCLUDE_DIRECTORIES(${BOOST_path})
INCLUDE_DIRECTORIES(${wxWidgets_INCLUDE_DIRS})


ADD_SUBDIRECTORY(wxOgrePanel)
#ADD_SUBDIRECTORY(OgreDShow)
#ADD_SUBDIRECTORY(Goblin)

EXPORT_LIBRARY_DEPENDENCIES(DEPENDENT.TXT)
